{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-only",
  "description": "Local Torgy UI capability. No remote URL is granted Tauri IPC access.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
